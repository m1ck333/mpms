{
  "buildCommand": "cd ../.. && pnpm install && cd apps/dashboard && vite build",
  "outputDirectory": "dist",
  "installCommand": "echo 'skip'",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
