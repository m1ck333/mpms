# MPMS

Manufacturing Process Monitoring System — a single monorepo.

- `be/` — .NET backend
- `fe/` — pnpm frontend workspace (dashboard + tablet + shared packages)

Branches: `staging` (→ alblue staging), `main` (→ algreen pilot).
